<% @page_title = "Add New Goal for #{@patient.name}" %>

<div class="my-form">
  <%= link_to "<< Go Back", patient_plan_of_cares_path(@patient) %>
  <div class="my-form__body">
    <%= render "form" %>
  </div>
</div>
